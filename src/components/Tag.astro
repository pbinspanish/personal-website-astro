---
const { tag } = Astro.props;
---

<a
  href={`/tags/${encodeURIComponent(tag)}`}
  title={tag}
  class="text-base-400 dark:text-base-500 hover:text-base-700 hover:dark:text-base-300 me-2 text-sm no-underline!"
>
  #{tag}
</a>
