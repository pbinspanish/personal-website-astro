---
import BaseCentered from "../components/BaseCentered.astro";
import Title from "../components/Title.astro";

interface Props {
  title: string;
  lang: string;
}

const { title, lang } = Astro.props;
---

<BaseCentered page_title={title} lang={lang}>
  <!-- Title -->
  <Title title={title} />

  <slot />
</BaseCentered>
